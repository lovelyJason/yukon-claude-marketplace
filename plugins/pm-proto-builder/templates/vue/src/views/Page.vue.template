<script setup>
import { ref } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'

// 页面：{{PAGE_TITLE}}

// 搜索表单
const searchForm = ref({
  // {{SEARCH_FIELDS}}
})

// 表格数据 (Mock)
const tableData = ref([
  // {{MOCK_DATA}}
])

const loading = ref(false)
const pagination = ref({ current: 1, pageSize: 10, total: 100 })

const handleSearch = () => ElMessage.info('搜索功能演示')
const handleReset = () => Object.keys(searchForm.value).forEach(k => searchForm.value[k] = '')
const handleAdd = () => ElMessage.info('新增功能演示')
const handleEdit = (row) => ElMessage.info(`编辑: ${row.id}`)
const handleDelete = (row) => ElMessageBox.confirm('确定删除?').then(() => ElMessage.success('删除成功'))
</script>

<template>
  <div>
    <!-- 搜索 -->
    <el-card shadow="never" style="margin-bottom: 16px">
      <el-form :model="searchForm" inline>
        <!-- {{SEARCH_FORM_ITEMS}} -->
        <el-form-item>
          <el-button type="primary" @click="handleSearch">查询</el-button>
          <el-button @click="handleReset">重置</el-button>
        </el-form-item>
      </el-form>
    </el-card>

    <!-- 表格 -->
    <el-card shadow="never">
      <template #header>
        <div style="display: flex; justify-content: space-between; align-items: center">
          <span>{{PAGE_TITLE}}</span>
          <el-button type="primary" size="small" @click="handleAdd">新增</el-button>
        </div>
      </template>

      <el-table :data="tableData" v-loading="loading" stripe>
        <!-- {{TABLE_COLUMNS}} -->
        <el-table-column label="操作" width="150" fixed="right">
          <template #default="{ row }">
            <el-button type="primary" link size="small" @click="handleEdit(row)">编辑</el-button>
            <el-button type="danger" link size="small" @click="handleDelete(row)">删除</el-button>
          </template>
        </el-table-column>
      </el-table>

      <div style="margin-top: 16px; text-align: right">
        <el-pagination
          v-model:current-page="pagination.current"
          :page-size="pagination.pageSize"
          :total="pagination.total"
          layout="total, prev, pager, next"
        />
      </div>
    </el-card>
  </div>
</template>
